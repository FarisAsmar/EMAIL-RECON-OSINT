[build]
builder = "nixpacks"

[deploy]
startCommand = "playwright install-deps && playwright install && uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
